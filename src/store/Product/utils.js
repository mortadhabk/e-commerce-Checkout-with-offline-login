// export const pushproducts = (currentproducts, newproducts) => {
//   //   const existingproducts = currentproducts.find(
//   //     (cartItem) => JSON.stringify(cartItem) == JSON.stringify(newproducts)
//   //   );
//   //   if (existingproducts) {
//   //     return currentproducts;
//   //   }
//   return [...currentproducts, newproducts];
// };
export const categoryId = (categories) => {};
